### Commands to run

```
# Install packages
pip install playwright
pip install tls_client
pip install pandas
pip install lxml
pip install ipykernel

# This installs the browser utils
playwright install
```